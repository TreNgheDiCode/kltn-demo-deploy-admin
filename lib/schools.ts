import { db } from "./db";

export const GetSchoolNames = async () => {
  try {
  } catch (error) {
    console.log(error);
    return null;
  }
};

export const GetSchoolsByName = () => {
  try {
  } catch (error) {
    console.log(error);
    return null;
  }
};
